{% extends 'base.html.twig' %}

{% block title %}Show Professeurs
{% endblock %}

{% block body %}
	<div class="row my-5">
		<div class="col-md-5 mx-auto">
			<div class="card">
				{% if professeur  %}
					<H2>Nom :
						{{professeur.nom}}
					</H2>
					<table class="table table-sm table-dark">
						<thead>
							<tr>
								<th class="text-success">ID</th>
								{# <th>Nom(pere)</th> #}
								<th class="text-success">Ajouter / Supprimer "Classe"</th>
							</tr>
						</thead>
						<tbody>

							<tr>
								<td class="text-success">{{ "%04d"|format(professeur.id)}}</td>
								{# <td>{{ prefesseur.nomFamille }}</td> #}
								<td>
									<a href="{{ path('professeur_updateC',{ id: professeur.id})}}" class="btn btn-sm btn-success">
										<i class="fas fa-user-plus"></i>

									</a>
									{# <a href="{{ path('prefesseur_update',{ id: prefesseur.id})}}" class="btn btn-sm btn-warning">
																																																																																																				<i class="fa fa-edit"></i>
																																																																																																			</a>
																																																																																																			<a href="{{ path('prefesseur_delete',{ id: prefesseur.id})}}" class="btn btn-sm btn-danger">
																																																																																																				<i class="fa fa-trash"></i>
																																																																																																			</a> #}
								</td>

							</tr>

						</tbody>
					</table>
				{% endif %}
				<div class="card">

					{% if professeur.getClasses()  %}
						<table class="table table-dark table-striped">
							<thead>
								<tr>
									<th>Nom de classe</th>
									{# <th class="text-center">Modifier | Efacer</th>
									<th></th> #}
								</tr>
							</thead>
							<tbody>
								{% for classe in professeur.getClasses() %}
									<tr>
										{# <td>{{ "%04d" | format(eleve.getprefesseur().id)  }}</td> #}
										{# <td><img src="{{asset('/uploads/images/classes/' ~ eleve.image) }}" alt="" width="60" height="50"></td> #}
										<td>{{ classe.nomClasse }}</td>
									
										{# <td> #}
											{# class="text-center"> #}
											{# <a href="{{ path('eleve_show',{id: eleve.id})}}" class="btn btn-sm btn-primary">
																																																								<i class="fa fa-eye"></i>
																																																							</a> #}
											{# <a href="{{ path('eleve_updateP',{ id: classe.id})}}" class="btn btn-sm btn-warning">
												<i class="fa fa-edit"></i>
											</a>
											<a href="{{ path('ClasseProf_delete',{ id: classe.id})}}" class="btn btn-sm btn-danger">
												<i class="fa fa-trash"></i>
											</a> #}
										{# </td> #}

									</tr>
								{% endfor %}
							</tbody>
						</table>
						<div class="card">
							<a href="{{ path('professeur_list')}}" class="btn btn-sm btn-primary">
								<i class="fa-solid fa-circle-arrow-left"></i>
							</a>
						{% endif %}

					</div>
				</div>
			</div>

		{% endblock %}
