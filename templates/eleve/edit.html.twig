{% extends 'base.html.twig' %}

{% block title %}Show Eleve

{% endblock %}

{% block body %}
	{% if eleve  %}
		<H1>
			{{ eleve.getParent().nomFamille }}
			{{ eleve.prenom }}</H1>

	{% endif %}

	<div class="row my-5">
		<div class="col-md-10 mx-auto">

			<div class="card">

				{% if eleve  %}
					<table class="table table-dark table-striped">
						<thead>
							<tr>
								<th>N°Inscription</th>
								<th>Nom de Famille</th>
								<th>Prenom</th>
								<th>Date de Naissance</th>
								<th>Sexe</th>
								<th></th>
							</tr>
						</thead>
						<tbody>

							<tr>
								<td>{{ "%04d" | format(eleve.getParent().id)  }}</td>
								<td>{{ eleve.getParent().nomFamille }}</td>
								<td>{{ eleve.prenom }}</td>
								<td>{{ eleve.dateNaissanceAt|date('d/m/Y') }}</td>
								{% if eleve.Sexe  %}
									<td>M</td>
								{% else  %}
									<td>F</td>
								{% endif %}
								<td>{# <a href="{{ path('eleve_add',{id_parent: parent.id})}}" class="btn btn-sm btn-success">
																																																							<i class="fas fa-user-circle"></i>
																																																						</a> #}
									{# <a href="{{ path('parent_update',{ id: parent.id})}}" class="btn btn-sm btn-warning">
																																																																																											<i class="fa fa-edit"></i>
																																																																																										</a>
																																																																																										<a href="{{ path('parent_delete',{ id: parent.id})}}" class="btn btn-sm btn-danger">
																																																																																											<i class="fa fa-trash"></i>
																																																																																										</a> #}
								</td>

							</tr>

						</tbody>
					</table>


					<div class="card">
						{% if show=='eleve'  %}
							<a href="{{ path('eleve_list')}}" class="btn btn-sm btn-primary">
								<i class="fa-solid fa-circle-arrow-left"></i>
							</a>
						{% else  %}
							<a href="{{ path('parent_edit',{id: eleve.getParent().id})}}" class="btn btn-sm btn-primary">
								<i class="fa-solid fa-circle-arrow-left"></i>
							</a>
						{% endif %}

					{% endif %}
				</div>
			</div>
		</div>

	{% endblock %}
